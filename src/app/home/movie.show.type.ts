export interface Movie { stream_id:number, name: string ,stream_icon: string, added: number }

export interface Show {series_id: number, name: string, cover: string, last_modified: string}

export interface SliderItem { id:number, title: string ,posterSrc: string }